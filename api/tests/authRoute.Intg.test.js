import app from '../app.js'
import supertest from 'supertest'

describe('POST /user signUp', () => {
    describe("given user is signed up", () =>{
        // should save user email and password to the database
        // should response with a json object containing the user id 
        // should response with a 200 status code
        // should specify json in the content type header 
    })
    describe("when user email and password are set", () =>{

    })
});
